target C {
    platform: "rp2040",
    threading: false,
    build-type: "Debug"
}

preamble {=
    #include <stdio.h>
=}

main reactor {
    timer t1(0, 1 sec); 
    reaction(t1) {=
        printf("Hello World!\n");
    =}
}
